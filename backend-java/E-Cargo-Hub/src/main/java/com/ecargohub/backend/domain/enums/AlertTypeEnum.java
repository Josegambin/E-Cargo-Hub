package com.ecargohub.backend.domain.enums;

public enum AlertTypeEnum {
    SPEED_LIMIT_EXCEEDED,
    EMERGENCY_BRAKING,
    VEHICLE_STOPPED,
    ROUTE_DEVIATION,
    COMMUNICATION_ERROR,
    SIMULATION_ERROR
}